# Deprecated Shadowdark Test Fixtures

These Shadowdark-specific scripts depend on historical local module assumptions, hard-coded world data, or pre-SDK paths. They are parked until the base platform and SDK boundary are stable enough to decide which module-specific regressions should become supported SDK tests.
