# Deprecated Module-Specific Tests

This directory holds old module-focused experiments that predate the current module SDK and registry lifecycle model. They are intentionally not part of the default test suite.

Keep them only as reference material while the base system is still being hardened. If a case is still valuable after SDK boundary work resumes, rewrite it against the public SDK/module contracts and move it into an active test directory.
